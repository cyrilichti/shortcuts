# Goals

This directory contains reusable completion criteria installed under
`.agents/goals/`.

Each goal defines one desired outcome, its measurable success criteria,
validation requirements, and stopping conditions. It describes what success
means, not how to reach it.

Implementation sequences belong in `../workflows/`; reusable capabilities
belong in `../skills/`.
