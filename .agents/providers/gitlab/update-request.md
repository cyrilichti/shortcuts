# update-request

Description replacement, draft removal, and ready-title update are unsupported
by the current verified adapter operations. Return `unsupported` with this
exact reason without calling a similarly named or inferred GitLab operation.
